//Write a c++ program to add two numbers taken from user.
#include<iostream>
#include<conio.h>
using namespace std;
int main()
{
    cout<<"Enter two numbers";
    int x,y;
    cin>>x>>y;
    cout<<"Addition of two numbers "<<x<<" and "<<y<<" is "<<x+y;
    getch();
}
///Note:- we can declare variable anywhere in c++ but before its use.
///       While in C, we have to declare all variable first B'coz after Declaration statement we can not write Action Statement
